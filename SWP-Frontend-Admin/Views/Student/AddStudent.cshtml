@model Student
@{
    ViewBag.Title = "Add a Student";
}

<h2>Add a Assignment <a asp-action="Index" class="btn btn-sm btn-secondary">Back</a></h2>
<form asp-action="AddStudent" method="post">
    <div class="form-group">
        <label for="Id">Id:</label>
        <input class="form-control" name="Id" />
    </div>
    <div class="form-group">
        <label for="Name">Name:</label>
        <input class="form-control" name="Name" />
    </div>
    <div class="form-group">
        <label for="Main">Main:</label>
        <input class="form-control" name="Main" />
    </div>
  @*  <div class="form-check">
        <label class="form-check-label" for="Leader">Is Leader? </label>
        <input type"
        <input class="form-check-input" type="checkbox" value=true name="Leader" />
    </div>*@
    @*<ng-form>
        <label>Is Leader?</label>
        <input type="hidden" value="false" name="Leader"/>
        <input type="checkbox" value="true" name="Leader"/>
    </ng-form>*@
    <label for="Leader">Is Leader? </label>
    <select name="Leader" id="Leader">
        <option value=true>Yes</option>
        <option value=false>No</option>
    </select>
    
    <div class="text-center panel-body">
        <button type="submit" class="btn btn-sm btn-primary">Add</button>
    </div>
</form>

@*<h3 class="alert">@ViewBag.Result</h3>

@if (Model != null)
{
    <h2>Student</h2>
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Main</th>
                <th>Leader</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.Id</td>
                <td>@Model.Name</td>
                <td>@Model.Main</td>
                <td>@Model.Leader</td>
            </tr>
        </tbody>
    </table>
}*@