@model Team
@{
    ViewBag.Title = "Add a Team";
    Layout = "_LectureLayout";
    List<Topic> TopicList = (List<Topic>)ViewData["TopicList"];

}
<div class="form_container">
    <a asp-action="CurrentCourseTeam" class="back" style="    position: relative;
    left: 3%;font-size:20px;font-weight:800;text-decoration:none;color:black;">&lt- </a>
<form asp-action="CurrentCourseTeamAdd" method="post" >

    <h3>Add team into course</h3>
        <div class="form-group d-flex flex-column justify-content-between">
        <div>
            <label for="TeamId">ID</label>
            <input class="form-control" name="TeamId" required  />
        </div>
    </div>
    <br />
@*    <select class="form-select form-select-lg" name="TopicId" id="TopicId" required>
        <option selected>Topic: </option>
        @{
            foreach (Topic r in TopicList)
            {
                <option value="@r.Id"> @r.Id: @r.Name</option>
            }
        }
        }
    </select>*@

    <div class="text-center panel-body">
        <button type="submit" class="btn btn-sm">Add</button>
    </div>
</form>
</div>
@if (ViewBag.Result == "Duplicate")
{
    <br />
    <h3 style="color:red" class="text-center">This Id already exists</h3>
}
