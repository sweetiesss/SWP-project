@model AssignmentStudent
@{
    ViewBag.Title = "Update an Assignment - Student";
    AssignmentStudent assignmentStudent = (AssignmentStudent)ViewData["AssignmentStudent"];
    List<Assignment> AssignmentList = (List<Assignment>)ViewData["AssignList"];
    List<Student> StudentList = (List<Student>)ViewData["StudentList"];
}

<h2>Update a Assignment <a asp-action="Index" class="btn btn-sm btn-secondary">Back</a></h2>
<form asp-action="UpdateAssignmentStudent" method="post">
    <select class="form-select form-select-lg" name="TaskId" id="TaskId">
        <option selected>@Model.TaskId</option>
        @{
            foreach (Assignment r in AssignmentList)
            {
                <option value="@r.Id"> @r.Id</option>
            }
        }
        }
    </select>
    <br />
    @*<label for="StudentId">Student Id: </label>*@
    <select class="form-select form-select-lg" name="StudentId" id="StudentId">
        <option selected>@Model.StudentId</option>
        @{
            foreach (Student r in StudentList)
            {
                <option value="@r.Id"> @r.Id</option>
            }
        }
    @*<div class="form-group">
        <label asp-for="@Model.TaskId">Id:</label>
        <input class="form-control" asp-for="@Model.TaskId" />
    </div>
    <div class="form-group">
        <label asp-for="StudentId">Name:</label>
        <input class="form-control" asp-for="StudentId" />
    </div>*@
    <div class="text-center panel-body">
        <button type="submit" class="btn btn-sm btn-primary">Update</button>
    </div>
</form>

@if (ViewBag.Result == "Success")
{
    <div>button run ok</div>
    @*   <h2>Assignment</h2>
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Start Location</th>
                <th>End Location</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                       <td>@Model.Id</td>
            <td>@Model.Name</td>
            <td>@Model.Description</td>
            <td>@Model.DateStart</td>
                <td>@Model.DateEnd</td>
            </tr>
        </tbody>
    </table>*@
}